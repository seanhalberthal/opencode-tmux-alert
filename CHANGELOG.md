# Changelog

## [Unreleased]
- Add unit tests for all event routing, TMUX guard, script resolution, and error handling
- Add CI workflow (bun test + build on push/PR to main)
- Add release workflow with auto-versioning and npm trusted publishing
- Add Makefile with build, test, check, clean, fmt, and lint targets
- Add test script to package.json
- Exclude test files from TypeScript compilation output
- Set initial version to 0.1.0
